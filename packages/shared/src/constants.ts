/**
 * Maximum number of rows supported
 */
export const MAX_ROWS = 1_048_576;

/**
 * Maximum number of columns supported
 */
export const MAX_COLUMNS = 16_384;

/**
 * Default column width in pixels
 */
export const DEFAULT_COLUMN_WIDTH = 100;

/**
 * Default row height in pixels
 */
export const DEFAULT_ROW_HEIGHT = 24;

/**
 * Minimum column width in pixels
 */
export const MIN_COLUMN_WIDTH = 20;

/**
 * Minimum row height in pixels
 */
export const MIN_ROW_HEIGHT = 16;

/**
 * Maximum column width in pixels
 */
export const MAX_COLUMN_WIDTH = 500;

/**
 * Maximum row height in pixels
 */
export const MAX_ROW_HEIGHT = 500;

/**
 * Number of rows/columns to render as buffer beyond visible area
 */
export const VIRTUALIZATION_BUFFER = 5;

/**
 * Default undo stack depth
 */
export const DEFAULT_UNDO_STACK_SIZE = 100;

/**
 * Autosave interval in milliseconds
 */
export const AUTOSAVE_INTERVAL = 30_000;

/**
 * Maximum formula length
 */
export const MAX_FORMULA_LENGTH = 8_192;

/**
 * Maximum cell content length
 */
export const MAX_CELL_CONTENT_LENGTH = 32_767;
